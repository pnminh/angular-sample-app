import { AdvancedBackgroundStyleDirective } from './advanced-background-style.directive';

describe('AdvancedBackgroundStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvancedBackgroundStyleDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
